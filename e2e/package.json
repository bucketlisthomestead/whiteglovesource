{
  "name": "wgds-e2e",
  "private": true,
  "scripts": {
    "test:features": "playwright test --config playwright.features.config.ts",
    "test:deploy-smoke": "playwright test deploy-smoke.spec.ts --config playwright.deploy.config.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
